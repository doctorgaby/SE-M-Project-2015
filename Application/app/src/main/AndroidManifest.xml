<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="group8.com.application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".UI.ResultsView"
            android:label="@string/points_grapher"></activity>

        <activity
            android:name="org.achartengine.GraphicalActivity"
            android:label="Bar Chart"></activity>

        <activity
            android:name=".UI.Splash"
            android:label="@string/app_name" >>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UI.mainView.menuView"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".UI.DrivingView.DrivingView"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".UI.RankingView"
            android:label="@string/app_name"></activity>

        <activity android:name=".UI.ChartActivity"
            android:label="@string/app_name"
            android:theme="@style/CustomTheme"></activity>
            
            
        <activity
            android:name=".UI.MedalsView"
            android:label="Achievements"></activity>


        <activity android:name=".alert.BrakesActivity" />
        <activity android:name=".alert.DistractionActivity" />
        <activity android:name=".alert.FuelActivity" />
        <activity android:name=".alert.SpeedActivity" />

    </application>

</manifest>
